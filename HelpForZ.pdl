class InitPdl
void SearchDish(String path);根据参数指向的路径，搜索该文件夹下所有文件及其文件夹。
如果是文件夹。1.递归。2.一级子目录存在电子书文件夹格式时，以父目录名字（字符串替换处理）为文件名建立文件。
记录下该文件夹内所有电子书格式文件名字。
void Classify(String filename);根据参数指向的文件夹名，分类处理。